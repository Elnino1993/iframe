// Where the widget takes its data from. Set this to your FaveRadar server before uploading the block.
// Empty string = the same domain the block is served from.
window.FAVERADAR_WIDGET = {
  api: 'https://www.faveradar.com',
};
